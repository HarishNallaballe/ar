server:
  port: 2002

app:
  messages:
       RegistrationSuccess : Application Registration Success
       RegistrationFailed : Application Registration was Failed, This HIS Plans for only Rhode Island Citizen
       
spring:
     datasource:
         password: abcd
         url: jdbc:mysql://ardb:3306/ardb
         username: root
         driverClassName: com.mysql.cj.jdbc.Driver
     jpa:
        hibernate:
           ddl-auto: update
           show-sql: true
           sql:
             init:
               mode: always
     application:
            name : AR-API
   